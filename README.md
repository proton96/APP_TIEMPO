# APP_TIEMPO

En esta aplicacion vamos a crear una app del tiempo la cual almacenara mediante un JSON,
valores que seran los que apareceran cuando busquemos una ciudad especifica. Claramente esto 
se realiza de manera local. 

Para ello vamos 